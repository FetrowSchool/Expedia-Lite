# Part 2 seeding and booking IDs

The user requested one-time SQLite seeding that preserves supplied IDs, never duplicates or restores starter rows on restart, and never overwrites user-created bookings. New bookings must use unique IDs beyond the seeded examples and retain those IDs after refresh and application restart.
