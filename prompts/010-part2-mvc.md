# Part 2 MVC architecture

The user requested clear Model–View–Controller separation without changing city search or redesigning the UI. Models describe hotel, trip, user, and booking data and relationships; Vue remains the View; a database controller owns SQLite CRUD; and FastAPI serves as the communication layer.
