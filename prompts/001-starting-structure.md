# Starting structure

The user requested:

> Create the starting structure for Assignment 1. Add `backend/` for FastAPI, `frontend/` for Vue, `README.md` for the goal and setup, and `AGENTS.md` for project rules. Do not install dependencies yet.

This produced the initial FastAPI/Vue repository scaffold before the current city-search implementation was added.
