# Part 2 booking history

The user requested a FastAPI endpoint that reads booking history from SQLite and a Vue table showing booking, user, trip/hotel, and status information. Seeded and newly created bookings must appear, empty history must be clear, and history must persist after browser refresh. Vue must access data only through FastAPI.
