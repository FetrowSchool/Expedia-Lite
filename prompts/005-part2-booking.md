# Part 2 booking flow

The user requested a Vue search→select→book flow backed exclusively by FastAPI and SQLite. The backend must use existing users, trips, and hotels, generate unique booking IDs using the existing convention, handle invalid data, persist across refreshes, and leave Part 1 search working. Booking history and other CRUD behavior were not requested in this step.
